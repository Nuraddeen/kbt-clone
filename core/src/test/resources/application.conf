kabuto {
    test-postgres{
      user=postgres
      password=password
      database=kabutotestdb
      port=5432
      serverName=localhost
      connectionTimeout=30000
    }

    base-files-directory = "/home/kabuto/tests/dms/files/images/"
}
